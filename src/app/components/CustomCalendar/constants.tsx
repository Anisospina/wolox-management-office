import {getMonthNames, getDayLetter} from './utils';

export const CONFIG_CALENDAR = {
  monthNames: getMonthNames(),
  dayNamesShort: getDayLetter()
};

export const CALENDAR_CONSTS = {
  left: 'left',
  period: 'period'
};
